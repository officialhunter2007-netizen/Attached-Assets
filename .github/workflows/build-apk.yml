name: Build Android APKs

on:
  push:
    branches: [main]
    paths:
      - 'apps/nukhba-student/**'
      - 'apps/nukhba-admin/**'
      - '.github/workflows/build-apk.yml'
  pull_request:
    branches: [main]
  workflow_dispatch:

# ── تطبيق الطالب (TWA) ────────────────────────────────────────────────────────
jobs:
  build-student:
    name: "📱 Student APK (TWA)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Build Debug APK
        working-directory: apps/nukhba-student
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace --info 2>&1 | tail -50

      - name: Print debug keystore fingerprint
        run: |
          echo ""
          echo "┌─────────────────────────────────────────────────────┐"
          echo "│  Debug Keystore SHA-256 Fingerprint (للتطوير فقط)  │"
          echo "└─────────────────────────────────────────────────────┘"
          keytool -list -v \
            -alias androiddebugkey \
            -keystore ~/.android/debug.keystore \
            -storepass android -keypass android \
            | grep "SHA256" || echo "(Debug keystore not found)"
          echo ""
          echo "⚠️  لإنتاج TWA حقيقي، ضع هذه القيمة في:"
          echo "   artifacts/nukhba/public/.well-known/assetlinks.json"

      - name: Upload Student APK
        uses: actions/upload-artifact@v4
        with:
          name: nukhba-student-debug
          path: apps/nukhba-student/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
          if-no-files-found: error

  # ── تطبيق الأدمن (WebView) ────────────────────────────────────────────────
  build-admin:
    name: "🛡️ Admin APK (WebView)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Build Debug APK
        working-directory: apps/nukhba-admin
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace --info 2>&1 | tail -50

      - name: Upload Admin APK
        uses: actions/upload-artifact@v4
        with:
          name: nukhba-admin-debug
          path: apps/nukhba-admin/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
          if-no-files-found: error

  # ── Release signing (يُفعَّل عند إضافة GitHub Secrets) ──────────────────
  #
  # لتفعيل بناء الإنتاج:
  # 1. أنشئ keystore:
  #    keytool -genkey -v -keystore nukhba.jks -alias nukhba -keyalg RSA -keysize 2048 -validity 10000
  # 2. حوّله لـ base64:
  #    base64 nukhba.jks
  # 3. أضف GitHub Secrets في Settings → Secrets → Actions:
  #    - KEYSTORE_BASE64: القيمة من الخطوة 2
  #    - KEYSTORE_PASSWORD
  #    - KEY_ALIAS: nukhba
  #    - KEY_PASSWORD
  # 4. أزل التعليق عن الـ job أدناه

  # build-release:
  #   name: "🚀 Release APKs"
  #   runs-on: ubuntu-latest
  #   needs: [build-student, build-admin]
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with: { java-version: '17', distribution: 'temurin' }
  #     - uses: gradle/actions/setup-gradle@v3
  #       with: { gradle-version: '8.5' }
  #     - uses: android-actions/setup-android@v3
  #     - name: Decode keystore
  #       run: echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > nukhba.jks
  #     - name: Build Student Release
  #       working-directory: apps/nukhba-student
  #       env:
  #         KEYSTORE_PATH: ../../nukhba.jks
  #         KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
  #         KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
  #         KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
  #       run: chmod +x gradlew && ./gradlew assembleRelease
  #     - name: Build Admin Release
  #       working-directory: apps/nukhba-admin
  #       env:
  #         KEYSTORE_PATH: ../../nukhba.jks
  #         KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
  #         KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
  #         KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
  #       run: chmod +x gradlew && ./gradlew assembleRelease
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: nukhba-release-apks
  #         path: |
  #           apps/nukhba-student/app/build/outputs/apk/release/*.apk
  #           apps/nukhba-admin/app/build/outputs/apk/release/*.apk
