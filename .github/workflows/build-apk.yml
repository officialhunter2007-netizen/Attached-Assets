name: Build Android APKs

on:
  push:
    branches: [main]
    paths:
      - 'apps/nukhba-student/**'
      - 'apps/nukhba-admin/**'
      - '.github/workflows/build-apk.yml'
      - '.github/signing/**'
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-student:
    name: "📱 Student APK"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Decode keystore
        run: |
          base64 -d .github/signing/nukhba.keystore.b64 > /tmp/nukhba.keystore
          echo "Keystore decoded ✅"

      - name: Build Release APK (signed)
        working-directory: apps/nukhba-student
        env:
          KEYSTORE_PATH: /tmp/nukhba.keystore
          KEYSTORE_PASSWORD: nukhba2024
          KEY_ALIAS: nukhba
          KEY_PASSWORD: nukhba2024
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --stacktrace

      - name: Upload Student APK
        uses: actions/upload-artifact@v4
        with:
          name: nukhba-student
          path: apps/nukhba-student/app/build/outputs/apk/release/app-release.apk
          retention-days: 90
          if-no-files-found: error

  build-admin:
    name: "🛡️ Admin APK"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: '8.5'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Decode keystore
        run: |
          base64 -d .github/signing/nukhba.keystore.b64 > /tmp/nukhba.keystore
          echo "Keystore decoded ✅"

      - name: Build Release APK (signed)
        working-directory: apps/nukhba-admin
        env:
          KEYSTORE_PATH: /tmp/nukhba.keystore
          KEYSTORE_PASSWORD: nukhba2024
          KEY_ALIAS: nukhba
          KEY_PASSWORD: nukhba2024
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --stacktrace

      - name: Upload Admin APK
        uses: actions/upload-artifact@v4
        with:
          name: nukhba-admin
          path: apps/nukhba-admin/app/build/outputs/apk/release/app-release.apk
          retention-days: 90
          if-no-files-found: error
