{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "deploy": "bash docker/deploy.sh",
    "deploy:ssl": "bash docker/deploy.sh --ssl",
    "deploy:update": "bash docker/deploy.sh --update",
    "dev": "docker-compose up --build",
    "logs": "docker-compose logs -f",
    "logs:api": "docker-compose logs -f api",
    "logs:nginx": "docker-compose logs -f nginx",
    "logs:db": "docker-compose logs -f db",
    "stop": "docker-compose down",
    "restart": "docker-compose restart",
    "clean": "docker-compose down --volumes --remove-orphans && docker system prune -f",
    "health": "docker-compose ps && curl -s http://localhost/api/healthz || echo 'Services not ready yet'"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
