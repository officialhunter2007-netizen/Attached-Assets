<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- الإنترنت مطلوب لتحميل المنصة -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- صلاحيات الكاميرا والميكروفون للجلسات الصوتية وتحميل الصور -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- الإشعارات (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NukhbaStudent"
        android:usesCleartextTraffic="false">

        <!--
          LauncherActivity من مكتبة androidbrowserhelper تُشغّل التطبيق
          كـ Trusted Web Activity (TWA) داخل Chrome.
          هذا يضمن عمل Service Worker والإشعارات الفورية تماماً كالمتصفح.
        -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:screenOrientation="unspecified">

            <!-- نقطة الدخول الرئيسية للتطبيق -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Verified Deep Links — يتطلب صحة ملف assetlinks.json
              على https://learnukhba.com/.well-known/assetlinks.json
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="learnukhba.com" />
            </intent-filter>

            <!-- URL الافتراضي عند فتح التطبيق -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://learnukhba.com" />

            <!-- لون شريط الحالة — ذهبي نُخبة #C8A84B -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR_DARK_MODE"
                android:resource="@color/colorPrimaryDark" />

            <!-- Splash Screen -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/ic_launcher_foreground" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/colorBackground" />
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_FADE_OUT_DURATION"
                android:value="300" />

            <!-- إذا لم يكن Chrome مثبتاً، افتح بـ WebView -->
            <meta-data
                android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="webview" />

            <!-- نطاقات إضافية مسموح بها داخل التطبيق -->
            <meta-data
                android:name="android.support.customtabs.trusted.ADDITIONAL_TRUSTED_ORIGINS"
                android:value="" />
        </activity>

        <!-- خدمة TWA المطلوبة لمكتبة androidbrowserhelper -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.DelegationService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

    </application>

</manifest>
