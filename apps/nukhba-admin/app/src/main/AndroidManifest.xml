<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NukhbaAdmin"
        android:usesCleartextTraffic="false">

        <!--
          تطبيق الأدمن يستخدم WebView بدلاً من TWA لمزيد من التحكم:
          - يمنع فتح روابط خارجية
          - يتحقق من أن المستخدم على صفحة /admin
          - يدعم رفع الملفات والكاميرا وتشغيل الصوت
        -->
        <!-- FCM service — receives push notifications for new subscription requests -->
        <service
            android:name=".NukhbaFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboardHidden">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links لصفحة الأدمن -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                    android:host="learnukhba.com"
                    android:pathPrefix="/admin" />
            </intent-filter>

        </activity>

    </application>

</manifest>
